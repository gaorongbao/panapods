# PanaPods v1.0.174 — 清理与重构记录

## 一、工作区清理：1.66 GB → 417 MB（回收 1.4 GB）

| 项目 | 回收 | 说明 |
|---|---|---|
| 旧版本 APK ×35 | ~98 MB | 只保留 171 / 172 / 173 |
| `_rom/tools` | 780 MB | jadx 反编译源码，随时可从 APK 重解；保留 4 个系统 APK 备查 |
| `.diag` | 444 MB | 一次性诊断日志快照与 dex 解包产物 |
| `app/build` | 181 MB | Gradle 构建缓存 |
| 崩溃日志 + lspd db + bt_dump | ~12 MB | 均可重建/重抓 |

> 项目无 git，删除不可逆 → 重构前已整包备份到 `_backup_pre_v174_refactor/`（74 个文件）。

## 二、代码整理（Android 模块，14 470 行）

### 1. 死代码删除（约 200 行，全部经全库 grep 复核零引用）
- **hook**：`samePanaOui` / `isLikelyPanaByAddr` / `readHeadsetInfoAddress` / `nonPanaAddresses` / `hiddenLeAddresses`（只写不读）/ `forceAncestorCardVisible` / `lastForceAncLogAt` / `RETRY_MAX_TICKS`+`retryTicks`（声明了却从不比较，重试循环实际无上限）/ `walkChildren(maxDepth)` 重载 / `scanSettingsApkClasses` / `currentName`
- **protocol**：15 个从未调用的协议命令方法（`getAdaptiveAnc`…`findMe`；`getAmbientMode`/`setAmbientMode` 在用，保留）
- **ble / bridge**：`getState` / `reconnect` / `getMtu` / `isWriting` / `scheduleAutoReconnect` / `EXTRA_STATE_JSON` / `getClassicPanaDevice` / `markAsHiddenLeAddress` / `isHiddenLeAddress` / `DIAGNOSTICS_URI` / `MAX_RETRY` / TRSPX 与 SPP UUID
- **其它**：`soundModeText` / `ncLevelDb` / `ConfigManager` 5 个未用配置项 / `RootKeepAlive` 2 项 / `ScopeRestarter` 单参重载 / `SectionCard` / `SectionTitle` / `VerticalSpacer`

### 2. 真实 bug 修复
1. **电量 target 无符号比较**：`BatteryStateTracker.onBatteryReceived` 原用 `when (target.toByte())`，target 实际是 0..255，≥128 时翻成负数永远匹配不上常量 → 主耳电量可能永远不上报。改为 `target and 0xFF`，并补回归测试（0x81 必须被拒）。
2. **协议在途命令被误删**：`handleIndication` 先 `completeCmd(raceId)`，未匹配分支又回落 `handleResponse()` 二次 `completeCmd` → 同一包把队列里第二条命令一起出队。给 `handleResponse` 加 `complete` 参数，回落时传 `false`。
3. **蓝牙关闭时崩溃**：`AirohaBleClient.connectInternal` 里 `(getSystemService(...) as BluetoothManager).adapter` 强转 + 非空，蓝牙关闭会抛 CCE/NPE → 改安全取值 + `isEnabled` 校验。

### 3. 性能优化（热路径）
- 电量回调里的**全字段反射 dump**、`setVisibility` 的 **stackTrace**、参数 `joinToString` 全部加 `PanaLog.enabled` 守卫（`PanaLog.d(msg: String)` 的参数会先求值，不守卫照样浪费）。
- `resId → 资源名` 加 `ConcurrentHashMap` 缓存，避免每次 `setImageResource` 查资源表。
- HomePage 首帧不再在主线程打开模块 APK 读 ZipFile（改 IO 线程异步）。
- 底部导航 `tabs` 用 `remember` 复用，避免每次重组新建 list 导致导航无法 skip。

### 4. 使用体验
- 设备页：加载中显示进度条（不再先闪"没有找到已配对设备"），空态给「重新扫描」入口。
- 断开按钮：点击后变"断开中"并置灰，防连点重复断开。
- ANC 选择器：未连接时补提示"耳机未连接，连接后可切换降噪 / 环境声"。

## 三、验证
- `compileReleaseKotlin` 通过
- `testDebugUnitTest`：**29 个用例，0 失败**（含新增的无符号比较回归用例）
- `assembleRelease` 成功，`aapt2 dump badging` 校验：`versionCode=174 / versionName=1.0.174`
- dex 校验：`com.panapods.hook.HookEntry` 在 `java_init.list` 内；配置键 `swap_ear_sides` / `hide_from_recents` / `root_keepalive` / `debug_log_enabled` / `panapods_config` 全部存活（未被 R8 裁掉）

## 四、未处理（后续可选）
- hook 里 22 处重复 `Class.forName("android.app.ActivityThread")`，未做缓存
- `PanaBridge.isPana` / `isPanaDevice` 是纯别名；`isSameDeviceAddress` / `isAddressSibling` 近似重复
- UI 文案大量中文硬编码，未统一走 `strings.xml`
- `Theme.textSecondary` 浅色下对比度偏低
- `_dexparse*.py` / `_suiparse.py` 等 7 个一次性逆向脚本仍在根目录
